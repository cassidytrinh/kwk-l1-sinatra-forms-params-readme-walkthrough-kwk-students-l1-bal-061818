<!DOCTYPE html>
<html>
  <head>
    <title>Results!</title>
    <link rel="stylesheet" href="stylesheets/style.css" type="text/css">
  </head>
  <body>
    <!-- <h1 class="Header">PLACES TO GO</h1> -->
    <header class="banner-header" role="banner">
      <img src="http://i1262.photobucket.com/albums/ii601/shaygyawali/NOWGO-01_zpsnc1xpttr.jpg">
      <!-- <img src="https://drive.google.com/open?id=0B18PBIepsQRXNUplNWFPc3NHOEZ1cGt3aTZMRVBFSm5Sa3g0"> -->
    </header>
    <h2>Here are some recommendations on where to go based on your answers:</h2>
    <% @results= [] %>
    <% @places.each do |place| %>
      <% if place.city.include?(@city.to_s) == true && place.age.include?(@age.to_s) == true && place.category == @category %>
        <% if (@accom & place.accom) == @accom %>
          <% @results.push(place) %>
        <% end %>
      <% end %>
    <% end %>
    <% @results.each do |result| %>
      <h3 class="result_title"><%= result.name %></h3>
      <p><%= result.description %><a href= <% result.link %>> Learn More</a></p>
    <% end %>
  </body>
</html>
